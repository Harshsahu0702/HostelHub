# Local MongoDB connection string (use this when running MongoDB locally)
MONGO_URI=mongodb+srv://nikhilkr8967_db_user:dYTA7xmQ2nKmMRFK@hsm.78umja1.mongodb.net/HSM?retryWrites=true&w=majority

# Server PORT (optional - server.js currently falls back to 5000 if not set)
PORT=5000

# Environment (optional)
NODE_ENV=development

# If your MongoDB instance uses authentication, replace the URI with:
# MONGO_URI=mongodb://<username>:<password>@127.0.0.1:27017/hostel-management

JWT_SECRET=super_secret_key_123
JWT_EXPIRES_IN=7d